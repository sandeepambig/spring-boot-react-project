spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hotelbookingapp
    username: root
    password: admin
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      format-sql: true
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB
      file-size-threshold: 2MB

